{
  "name": "ksef-pdf-web2",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build:fonts": "node scripts/extract-fonts.mjs",
    "predev": "npm run build:fonts",
    "prebuild": "npm run build:fonts",
    "postinstall": "npm run build:fonts",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "pdfmake": "^0.2.10"
  },
  "devDependencies": {
    "@types/pdfmake": "^0.2.9",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
